<%= form_for(sales_summary) do |f| %>
  <% if sales_summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sales_summary.errors.count, "error") %> prohibited this sales_summary from being saved:</h2>

      <ul>
      <% sales_summary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tasters %>
    <%= f.number_field :tasters %>
  </div>

  <div class="field">
    <%= f.label :sales_volume %>
    <%= f.number_field :sales_volume %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
